#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
source ../bin/bootstrap_renderer.sh

echo "--- format --check"
gleam format --check

echo "--- deps"
gleam deps download

echo "--- ensure renderer"
ensure_downloaded_renderer

echo "--- build"
gleam build

echo "--- test"
gleam test

echo "--- all clear"
